# 铁路信号产品场景需求测试集

## 用途

用于验证场景需求分析器对铁路信号领域专业场景的结构化需求提取 + Mermaid 图表生成效果。

## 难度分级

| 级别 | 说明 | 预期 FR 数量 |
|------|------|-------------|
| ★☆☆ 简单 | 单设备、单功能，流程线性 | 2-3 条 |
| ★★☆ 中等 | 多设备协同，含分支/判断 | 3-5 条 |
| ★★★ 困难 | 多子系统交互，含异常处理 | 4-6 条 |

---

## 一、计算机联锁系统（CBI）

### TC-001 站内列车进路建立 ★☆☆

**场景描述：**
列车进入车站，需要从 3 股道经由 6 号道岔反位进入 4 股道。联锁系统需要检查 4 股道空闲、6 号道岔可转动至反位、敌对进路未建立。满足条件后操纵道岔转换到反位并锁闭，信号机开放。

**预期输出：**
- 图表类型：stateDiagram-v2 或 flowchart
- 核心 FR："站内进路建立与锁闭控制"、"道岔转换与锁闭管理"、"敌对进路冲突检测"
- 难度验证：单进路，3 项基本联锁条件检查

### TC-002 进路解锁与取消 ★★☆

**场景描述：**
调度员建立了一条从 5 股道经 8 号道岔定位至 3 股道的接车进路，信号机已开放。此时邻站通报该列车晚点 20 分钟。调度员需要取消该进路：首先关闭信号机，等待接近区段无车占用后，依次解锁道岔、解锁进路区段。若列车已进入接近区段，需执行人工延时解锁（3 分钟延时）。

**预期输出：**
- 图表类型：stateDiagram-v2
- 核心 FR："进路正常取消与解锁"、"接车进路延时解锁"、"信号机关闭控制"
- 异常路径：列车已进入接近区段时的延时解锁

### TC-003 引导接车 ★★☆

**场景描述：**
车站 7 号道岔发生故障，失去表示，无法转换至需要位置。然而该列车必须在本站停靠。调度员决定改用引导接车方式：信号员按下引导按钮，引导信号亮白灯，列车以限速 20km/h 通过故障道岔位置进入站内。联锁系统此时不检查 7 号道岔位置，但仍需检查敌对进路未建立。

**预期输出：**
- 图表类型：flowchart（分支判断多）
- 核心 FR："引导接车控制"、"联锁条件降级管理"、"设备故障容错处理"

---

## 二、列控中心（TCC）

### TC-004 轨道电路编码 ★☆☆

**场景描述：**
TCC 实时采集轨道区段占用状态。当检测到某区间轨道电路被列车占用时，TCC 根据列车运行方向、前方区段占用状态和联锁进路，自动计算并向该区段发送对应的低频编码信息（如 L5/L4/L3/L2/L1/LU/U/HU 码），通过轨道电路传输至车载设备。当前方第三区段空闲时发送 L5 码，前方第二区段空闲时发送 L3 码，前方仅一个区段空闲时发送 U 码。

**预期输出：**
- 图表类型：flowchart
- 核心 FR："轨道区段占用检测"、"低频码序自动计算与编码"、"基于前方区段状态的码序选择"
- 难度：逻辑链清晰，5 种码序条件

### TC-005 临时限速（TSR）处理 ★★★

**场景描述：**
线路维护部门报告，K120+500 至 K121+200 区段钢轨磨损超限，需设置 7 天临时限速 60km/h。调度员通过 CTC 向临时限速服务器下发限速命令。临时限速服务器验证调度员权限和限速参数合法性后，将限速命令发送至管辖该区段的前后两个 TCC。TCC 接收到限速命令后，通过有源应答器向经过列车发送临时限速报文。当列车越过限速区段终点后，TCC 自动取消该限速命令。

**预期输出：**
- 图表类型：sequenceDiagram（多方交互）
- 核心 FR："临时限速命令下发与验证"、"TCC 限速报文生成与应答器发送"、"限速区段自动取消"
- 涉及系统：调度员、CTC、TSRS、TCC、有源应答器、车载

### TC-006 区间改变运行方向 ★★☆

**场景描述：**
双线区间，下行线因施工封锁，需将上行线改为双向运行。调度员在 CTC 上发起改变运行方向操作。联锁系统检查改向条件：改向区段空闲、对方站未建立敌对进路、改向按钮操作正确。条件满足后，原发车站转为接车站，原接车站转为发车站。区间信号机方向随之改变，允许上行线反向行车。

**预期输出：**
- 图表类型：stateDiagram-v2
- 核心 FR："区间运行方向改变控制"、"改向条件安全检查"、"信号机方向切换"

---

## 三、无线闭塞中心（RBC）

### TC-007 列车注册与行车许可 ★★★

**场景描述：**
CTCS-3 级线路，一列装备 C3 车载设备的动车组从车站出发。列车经过第一个应答器组时，车载设备向 RBC 发送位置报告和注册请求（含列车数据：车长、最高速度、制动性能等）。RBC 验证列车数据、确认列车在管辖范围内后，向车载发送系统配置参数和行车许可（MA）。MA 包含目标距离、允许速度、坡度等信息。列车在 RBC 的 MA 控制下追踪运行。当列车位置与 MA 终点之间的距离缩减至设定阈值时，车载提前向 RBC 请求延长 MA。

**预期输出：**
- 图表类型：sequenceDiagram
- 核心 FR："列车注册与验证"、"行车许可（MA）生成与管理"、"MA 提前请求与延长"
- 涉及系统：车载、应答器、RBC、联锁

### TC-008 RBC 切换 ★★★

**场景描述：**
列车由 RBC1 的管辖区域驶向 RBC2 的管辖区域。当列车接近 RBC1/RBC2 切换边界时，RBC1 预先与 RBC2 建立通信。RBC1 向车载发送 RBC2 的呼叫信息（含 RBC2 的 GSM-R 号码）。车载建立与 RBC2 的无线连接，RBC2 向车载发送切换确认。当列车位置越过切换边界时，RBC1 终止对列车的控制，RBC2 接管。若切换通信失败，列车自动降级至 CTCS-2 级继续运行。

**预期输出：**
- 图表类型：sequenceDiagram
- 核心 FR："RBC 间切换预建立"、"车载切换执行"、"切换失败降级处理"
- 异常路径：通信失败降级至 C2

---

## 四、调度集中系统（CTC）

### TC-009 列车运行计划自动调整 ★★☆

**场景描述：**
CTC 系统按日班计划自动生成列车运行图。由于上行线 3 列旅客列车晚点 15 分钟，CTC 自动检测到发车冲突。系统根据列车等级（高铁旅客优先于普速客货混跑）自动调整发车顺序：将站内 5 股道原定 14:30 发车的货运列车推迟至 14:45 发车，让 14:35 到达的高铁列车优先使用 5 股道。调整方案经调度员确认后自动执行。

**预期输出：**
- 图表类型：flowchart
- 核心 FR："运行图自动调整"、"列车优先级调度"、"调度方案人工确认"
- 难度：多约束条件（列车等级、到发时间、股道占用）

### TC-010 车站分散自律控制 ★★☆

**场景描述：**
CTC 车站子系统在中心与车站通信中断后自动切换至分散自律模式。站内调车作业由车站值班员在本地操作终端完成。系统自动跟踪车列移动并更新站场显示。通信恢复后，车站子系统向中心同步断联期间的操作记录和站场状态数据，确认一致后切换回中心控制模式。

**预期输出：**
- 图表类型：stateDiagram-v2
- 核心 FR："分散自律模式切换"、"本地调车作业控制"、"断联数据恢复与同步"

---

## 五、信号基础设备

### TC-011 信号机点灯与灯丝监测 ★☆☆

**场景描述：**
车站某架进站信号机的主灯丝（红灯）断丝。信号机自动点亮副灯丝，同时灯丝继电器落下，向联锁系统发送主灯丝断丝报警。联锁系统在站场图中将该信号机的报警指示灯点亮，并生成维修工单。维修人员到场更换信号灯泡后，灯丝继电器重新吸起，报警自动消除。

**预期输出：**
- 图表类型：stateDiagram-v2
- 核心 FR："信号机点灯控制"、"灯丝切换与断丝检测"、"设备故障报警与工单管理"

### TC-012 轨道电路分路不良检测 ★★☆

**场景描述：**
雨季潮湿天气导致站内某轨道区段多次出现分路不良（列车占用时轨道继电器不落下）。联锁系统检测到该区段在列车占用时未正常出清，但在区段空闲后多次出现轨道继电器短时间落下又吸起的异常动作。系统判定该区段分路不良，向维修终端报警。维修人员到场进行钢轨打磨和道砟清洁后，通过测试确认分路恢复正常，系统清除分路不良标记。

**预期输出：**
- 图表类型：flowchart
- 核心 FR："轨道电路状态监测"、"分路不良智能诊断"、"故障维修闭环管理"

---

## 六、综合场景

### TC-013 车站全站联锁系统启动与自检 ★★☆

**场景描述：**
计算机联锁系统上电启动。系统按以下顺序执行：硬件自检（CPU、I/O 板卡、通信板卡）→ 软件加载与校验（CRC 检查）→ 同步与备用系通信互检 → 室外设备状态采集（信号机灯丝、道岔位置、轨道电路占用）→ 站场状态初始化 → 系统运行正常指示灯点亮。若自检发现问题（如 I/O 板卡故障、通信中断、道岔表示不一致），系统在维护终端显示具体故障信息，不允许开放信号。

**预期输出：**
- 图表类型：flowchart
- 核心 FR："系统上电自检管理"、"设备状态采集与一致性校验"、"故障诊断与安全锁定"

### TC-014 紧急情况下站内联锁降级控制 ★★★

**场景描述：**
站内联锁系统检测到与列控中心的通信接口完全中断。根据故障导向安全原则，系统自动将站内所有信号机切换至最限制状态（红灯），已建立的进路立即锁闭但不解锁（防止列车挤岔）。调度员确认站内无线通信仍正常后，通过语音通知司机以目视模式限速 15km/h 通过车站。联锁系统维持道岔锁闭和信号机红灯状态，直至通信恢复并手动确认安全。

**预期输出：**
- 图表类型：stateDiagram-v2
- 核心 FR："通信故障检测与安全降级"、"进路锁闭保持"、"人工确认恢复机制"
- 异常路径：通信中断后的全链路安全处理

---

## 七、评分指标

| 指标 | 说明 | 权重 |
|------|------|------|
| 场景覆盖率 | 覆盖 CBI/TCC/RBC/CTC/基础设备/CBTC/TACS 的比例 | 25% |
| 需求完整性 | FR 覆盖所有关键系统能力的比例 | 20% |
| 粒度合理性 | FR 为系统级能力而非 UI 操作步骤 | 15% |
| 图表类型匹配 | diagramType 选择是否贴合场景 | 15% |
| 异常路径覆盖 | 是否包含故障/降级等异常处理需求 | 10% |
| CBTC 子系统识别 | 是否正确识别 ATS/ATO/ATP/ZC/VOBC 角色 | 5% |
| TACS 分布式逻辑 | FR 是否覆盖资源申请→分配→锁定→释放周期 | 5% |
| 降级模式完整度 | 是否覆盖 3+ 级降级路径 | 5% |

---

## 八、CBTC（基于通信的列车控制系统）

### TC-015 移动闭塞条件下列车追踪运行 ★★★

**场景描述：**
两条 CBTC 列车在同一区间追踪运行。前车 VOBC 向 ZC 周期发送位置报告（含列车速度、行驶距离、定位误差包络）。ZC 将前车位置加上安全防护包络后计算出移动授权（MA）终点位置，发送给后车 VOBC。后车 VOBC 收到 MA 后，结合线路数据（坡度、曲线半径、限速）实时计算紧急制动曲线，确保在本车当前位置至 MA 终点之间留足安全制动距离。当后车接近 MA 终点至设定阈值（通常前方 50-100m）时，VOBC 自动向 ZC 请求延长 MA。若 ZC 在 MA 有效期内未收到后车位置报告（通信超时），立即删除该列车的 MA 并向其发送紧急制动命令。

**预期输出：**
- 图表类型：sequenceDiagram
- 核心 FR："列车位置周期报告"、"MA 计算与安全包络管理"、"超速防护曲线实时计算"、"MA 提前请求与延长"、"通信超时紧急制动"
- 涉及系统：前车VOBC、ZC、后车VOBC
- 安全关键：定位误差包络处理，最不利位置原则

### TC-016 CBTC 门安全联锁（列车门+屏蔽门联动）★★☆

**场景描述：**
CBTC 列车精确停稳在车站站台，列车门与屏蔽门对齐。VOBC 检测到列车速度为零且停在停车窗内（±0.3m），向 ZC 发送停稳/停准信号。ZC 确认列车已锁闭。站台侧 ZC 验证后发出站台门允许开启命令。VOBC 输出开门允许信号，ATS 同步触发站台门开启。所有站台门关闭锁紧后，ZC 验证屏蔽门锁闭信号，发出牵引使能命令。VOBC 收到牵引使能信号后允许司机发车。若任一列车门或站台门未锁紧，牵引使能被禁止，列车无法启动。

**预期输出：**
- 图表类型：sequenceDiagram
- 核心 FR："列车停准停稳检测"、"站台门联动开启/关闭控制"、"牵引使能安全条件检查"、"门未锁紧紧急制动防护"
- 涉及系统：VOBC、ZC、ATS、站台屏蔽门系统
- 关键验证：门安全联锁作为单原子FR不可拆分

### TC-017 列车定位失效降级处理 ★★★

**场景描述：**
正在 CBTC 模式下运行的列车在区间行驶时，VOBC 测速电机发生故障——A/B 通道测速值偏差超过安全阈值（>2%）。VOBC 立即切换到雷达+加速度计辅助定位模式，同时向 ZC 报告定位降级状态。由于定位不确定性增大，ZC 在计算 MA 时扩大安全防护包络，导致追踪间隔增加。列车运行至前方应答器组时，VOBC 通过应答器读取绝对位置（硬复位），重新校准定位系统。若经过 3 个应答器仍未校准成功，VOBC 判定定位永久丢失，触发紧急制动，列车停车后司机切换到限制人工驾驶模式（≤25km/h）继续运行至前方站。

**预期输出：**
- 图表类型：stateDiagram-v2
- 核心 FR："多传感器融合定位管理"、"定位降级识别与报告"、"安全防护包络动态调整"、"定位硬复位校准"、"定位永久丢失紧急制动与降级运行"
- 涉及系统：VOBC、ZC、应答器
- 异常路径：多级降级（传感器偏差→定位不确定性增大→定位丢失）

---

## 九、TACS（列车自主控制系统）

### TC-018 ATS 运行图自动调整与进路自动设置 ★★☆

**场景描述：**
早高峰两列 CBTC 列车在正线追踪运行。ATS 检测到前方列车因乘客上下车延误导致发车延迟 45 秒，后端列车接近该站时可能触发区间停车。ATS 自动调整运行计划：将前方列车发车时间后延 45 秒，通知后方列车进站时以 ATO 推荐速度减速进站（减少区间停车能耗）。同时 ATS 根据调整后的计划自动设置后方列车进路（道岔转换、信号开放），通过联锁接口下发进路命令。若道岔转换超时（>15s），ATS 重新选择替代进路，避免该列车在道岔区域长时间等待。

**预期输出：**
- 图表类型：flowchart
- 核心 FR："运行图冲突自动检测"、"ATO 推荐速度动态计算"、"进路自动设置与联锁接口"、"道岔转换超时替代进路选择"
- 涉及系统：ATS、ATO、联锁、VOBC

### TC-019 TACS 列车自主折返 ★★★

**场景描述：**
TACS 系统中，列车到达终点站完成乘客下车后，司机激活折返模式。列车 VOBC（TACS 自主单元）检测折返轨区段空闲（通过 T2T 查询对向无来车 + 检测折返轨占用状态）。VOBC 向 TSM（列车调度管理器）申请折返轨资源。TSM 分配折返轨资源并锁定。目标控制器 OC 执行道岔转换至折返位置，向 VOBC 确认道岔已锁闭。VOBC 自主计算折返 MA：反向牵引至折返轨停车点，换端后 MA 延伸至发车轨。列车反向进入折返轨停稳后，司机换端。换端完成后 TACS 系统自动计算前方 MA，准备发车。若 T2T 通信在过程中断续，系统降级至 TSM 集中仲裁模式，折返流程延长但安全不降级。

**预期输出：**
- 图表类型：sequenceDiagram
- 核心 FR："折返资源 T2T 检测与 TSM 申请"、"OC 道岔转换与锁闭确认"、"VOBC 自主折返 MA 计算"、"换端流程控制"、"T2T 降级 TSM 集中仲裁"
- 涉及系统：VOBC、TSM、OC、T2T模块
- 关键验证：TACS 分布式资源管理模型，T2T通信降级路径

### TC-020 CBTC→TACS 混合系统切换 ★★★

**场景描述：**
某地铁线路处于 CBTC→TACS 升级过渡期，部分列车仍为传统 CBTC 车载设备，部分列车升级为 TACS 车载设备。一列 CBTC 列车和一列 TACS 列车在同一区间追踪运行。TACS 列车通过 T2T 直接获取前车 CBTC 列车的位置（前车经 ZC 转发位置信息至 TSM，TSM 通过 T2T 网关转发给后车 TACS 列车）。TACS 列车 VOBC 自主计算 MA（基于 T2T 获取的前车位置），同时接收 ZC 同步发送的 CBTC 格式 MA 作为备份。当 T2T 通信正常时，TACS 列车优先使用自主 MA；当 T2T 断续超过阈值（500ms），立即切换至 ZC 集中式 MA 模式，运行连续性不受影响。

**预期输出：**
- 图表类型：flowchart（含分支判断）
- 核心 FR："CBTC/TACS 混合追踪模式控制"、"T2T 转发网关接口管理"、"双源 MA 融合与优先级选择"、"T2T 断续 ZC 后备自动切换"
- 涉及系统：CBTC 列车、TACS 列车、ZC、TSM、T2T网关
- 关键验证：混合系统场景的降级一致性

---

## 十、CBTC 进阶场景

### TC-021 ZC 越区切换（MA 连续性保持）★★★

**场景描述：**
CBTC 列车从 ZC1 管辖区域驶向 ZC2 管辖区域。当列车距切换边界约 500m 时，ZC1 检测到列车即将越区，主动与 ZC2 建立通信（RSSP-II 安全协议）。ZC1 将当前列车位置、速度、行驶方向及 MA 终点发送给 ZC2。ZC2 预计算新的 MA 并返回确认。列车位置越过边界时，ZC1 向车载发送 ZC2 的呼叫信息。车载 VOBC 断开与 ZC1 的连接，建立与 ZC2 的通信。ZC2 向列车发送预计算的 MA，确保 MA 连续性（无瞬间空白）。若 ZC2 通信建立超时（>3s），VOBC 维持 ZC1 的最终 MA 并以惰行模式滑行至边界安全停车，待通信恢复后重新获取 MA。

**预期输出：**
- 图表类型：sequenceDiagram
- 核心 FR："ZC 间越区预建立与安全通信"、"MA 连续性传递与预计算"、"车载 ZC 切换执行"、"切换超时惰行停车"
- 涉及系统：ZC1、ZC2、VOBC、DCS
- 异常路径：切换超时 MA 消失

### TC-022 CBTC 驾驶模式切换（CAM↔ATPM↔RM↔EUM）★★★

**场景描述：**
CBTC 列车日常运行涉及多种驾驶模式切换。正线正常运行使用 CAM（连续监督自动模式），VOBC 持续从 ZC 获取 MA，ATO 自动驾驶。当列车因施工进入临时限速区段（限速 20km/h、长度 300m），MA 终点的允许速度降至 0km/h 以下时，系统自动触发 CAM→ATPM（自动防护手动驾驶）切换，司机手动驾驶但 ATP 持续监控。列车到达维修区域，ATP 检测到前方轨旁设备未纳入 CBTC 控制，需降级至 RM（限制人工模式），司机以 ≤25km/h 限速手动驾驶，ATP 仅提供过速报警。若无线通信完全丢失超过 10s，VOBC 触发紧急制动，停车后司机确认安全并以 EUM（非限制人工模式）继续运行至最近车站。从 EUM 再升级回 CAM 必须满足：列车停稳、司机确认、手动选择模式、ZO 重新建立通信并获取 MA、ATP 验证通信正常且前车位置已知。

**预期输出：**
- 图表类型：stateDiagram-v2
- 核心 FR："CAM 自动运行控制"、"CAM↔ATPM 自动降级切换"、"ATPM→RM 人工模式降级"、"RM→EUM 紧急人工运行"、"EUM→CAM 安全升级恢复"
- 涉及系统：VOBC、ZC、ATO
- 关键验证：4 种模式转换条件 + 升级恢复的安全检查链

### TC-023 CBTC 车辆段列车唤醒与休眠 ★★☆

**场景描述：**
次日运营前，车辆段停泊区内一列 CBTC 列车需从休眠状态唤醒。列车唤醒首先进行 VOBC 上电自检：CPU 自检、测速传感器测试、无线通信模块自检、TOD（司机操作台）通信测试。自检通过后，VOBC 尝试与 ZC 建立 CBTC 无线连接（DCS 漫游从车辆段非安全无线网切换到正线安全无线网）。ZC 验证列车 ID 和注册信息后分配通信时隙。VOBC 通过应答器（若存在）或走行一段距离获取初始绝对位置。ZC 以该位置为参考计算初始 MA（限速进入正线）。列车出库完成。晚间运营结束后，列车回段。ZC 撤销对该列车的 MA，VOBC 将定位标记为无效并保存最后一周期位置到掉电保持存储器后关机。ZC 从已注册列车列表移除该列车。

**预期输出：**
- 图表类型：flowchart
- 核心 FR："VOBC 唤醒自检与初始化"、"DCS 区域漫游切换"、"ZC 注册与通信建立"、"初始定位获取与初始 MA 计算"、"停车休眠与状态保存"
- 涉及系统：VOBC、ZC、DCS、应答器
- 关键验证：非CBTC→CBTC 区域切换

### TC-024 CBTC 轮径磨损与定位误差校准 ★★☆

**场景描述：**
某 CBTC 列车已连续运营 3 个月未进行轮径校准。VOBC 测速电机累计脉冲数换算的走行距离与通过应答器组读取的绝对位置之间的偏差逐渐增大。当偏差超过 1% 时，VOBC 在维护日志记录轮径磨损警告。当偏差超过 2%（安全阈值），VOBC 激活定位不确定性增大标志，该标志导致 ZC 在 MA 计算中扩大安全防护包络 15%。列车运行至下一组应答器时，VOBC 基于两个应答器之间的已知距离反算实际轮径值，覆盖校准参数。若偏差超过 5%（最大容差范围），VOBC 判定定位系统不可信，触发紧急制动，需维护人员进行机械轮径测量和手动校准方^C解除制动。

**预期输出：**
- 图表类型：flowchart（含分级分支判断）
- 核心 FR："轮径偏差连续监视"、"定位不确定性动态标记"、"安全防护包络自适应调整"、"应答器自动校准"、"超限紧急制动与人工维护"
- 涉及系统：VOBC、ZC
- 关键验证：三级阈值（1% 警告 / 2% 防护增大 / 5% 紧急制动）

### TC-025 CBTC 站台列车越过停车窗控制 ★★★

**场景描述：**
CBTC 列车进站时因制动率不足（雨天轨面湿滑），实际停车位置超过站台停车窗（±0.3m）约 1.2m。VOBC 检测列车速度为零但未在停车窗内，不输出开门允许。VOBC 计算回退距离（1.2m，在允许回退极限 3m 范围内），向 ZC 发送回退请求。ZC 确认后方区段空闲（回退范围内无列车占用），向 VOBC 发送回退授权（后退速度 ≤5km/h，后退距离 ≤1.5m）。VOBC 输出回退允许，司机（或 ATO）执行回退。列车重新停准后 VOBC 验证零速 + 停车窗内，输出开门允许，流程恢复正常。若超出行停车窗超过 3m，VOBC 禁止回退，司机将车厢门解锁但仅允许在站台侧开门，完成乘客疏散后列车驶往下一站不停车。

**预期输出：**
- 图表类型：sequenceDiagram + flowchart（复杂分支）
- 核心 FR："停车窗内位置验证"、"回退条件安全检查与授权"、"回退执行与再停准"、"越限禁止回退与疏散运行"
- 涉及系统：VOBC、ZC、ATO、屏蔽门系统
- 异常路径：回退越限→禁止回退→乘客疏散→跳停

---

## 十一、TACS 进阶场景

### TC-026 TACS 列车唤醒与 T2T 网络初始化 ★★☆

**场景描述：**
次日早高峰前，一列具备 TACS 能力的列车在正线存车线唤醒。VOBC（TACS 自主单元）上电自检：CPU 自检、T2T 无线模块通信测试、定位传感器自检、目标控制器 OC 通信链路检测。自检通过后，VOBC 通过 T2T 广播发现消息，查询附近列车和 TSM。同一线路上的前一列车（已在运行）返回 T2T 应答，告知自身位置和占用的资源。TSM 收到广播后向该列车分配 TACS 资源管理域并同步当前线路资源状态表（各道岔/区段占用情况）。VOBC 根据最后一已知位置 + 轮径脉冲推算初始位置，向 TSM 报告。TSM 确认位置在非冲突区域后，向 VOBC 下发当前 MA 边界。列车进入 TACS 自主控制状态。若 T2T 广播 5s 内无应答（无相邻列车），VOBC 通过 OC 查询轨旁状态间接推算前方区段状态，低效但可用。

**预期输出：**
- 图表类型：sequenceDiagram
- 核心 FR："VOBC 唤醒自检与通信初始化"、"T2T 广播发现与邻近列车应答"、"TSM 资源域分配与状态同步"、"初始位置推算与确认"、"T2T 无应答 OC 后备查询"
- 涉及系统：VOBC、TSM、OC、T2T模块
- 关键验证：纯 TACS 初始化流程，无 ZC 参与

### TC-027 TACS 交叉冲突资源死锁与 TSM 仲裁 ★★★

**场景描述：**
一列 TACS 列车从下行线经交叉渡线向上行线折返，另一列 TACS 列车从上行线经同一交叉渡线向下行线折返。两列车 VOBC 各自 T2T 查询到交叉渡线区段空闲，几乎同时向 TSM 申请交叉渡线资源分配（申请时间差 50ms）。TSM 检测到两列车申请重叠于同一资源组（交叉渡线由 4 组道岔 + 3 个轨道区段组成），判定资源冲突。TSM 启动仲裁策略：根据列车优先级（正线载客折返优先级高于回段空车）和申请到达时间，裁定下行线载客列车获得资源锁定，上行线空车立即收到资源拒绝响应。被拒列车的 VOBC 根据规划路线自动计算替代路径：选择另一条渡线（距离增加 200m）或等待 90s 后重试。VOBC 在等待期间保持当前位置不动并持续将 T2T 占用通告发送给对向列车。资源释放后 TSM 通知等待列车重新申请。

**预期输出：**
- 图表类型：sequenceDiagram（密集交互）
- 核心 FR："资源冲突自动化检测"、"TSM 优先级仲裁与锁定"、"VOBC 替代路径自动规划"、"等待重试与 T2T 占用通告"
- 涉及系统：VOBC_A、VOBC_B、TSM、OC
- 关键验证：分布式资源冲突检测 + 集中式仲裁 + 双重路径管理

### TC-028 TACS 列车灵活编组（虚拟连挂）★★★

**场景描述：**
平峰时段，两列 TACS 短编组列车（每列 3 节编组）在同一区间追踪运行，间隔约 1.2km。调度中心 ATS 为提升线路运能和节能效率，发出虚拟编组指令。后车 VOBC 通过 T2T 向前车 VOBC 发送编组请求，包含：本车制动性能曲线、车长、当前速度。前车 VOBC 验证后车制动能力满足安全追踪需求（后车紧急制动距离 ≤ 前车 MA 安全余量），接受编组。两车间建立编组虚拟刚性连接：前车以当前速度匀速运行，后车进行 CACC（协同自适应巡航控制）精确追踪至目标间隔 10m。编组后两车作为一个逻辑单元运行：后车不再独立请求 MA，而是跟随前车的 MA；前车负责向 TSM 报告编组整体占用状态。ATS 显示编组为单一列车图标。解编时，前车发出解编预告，后车减速拉开至安全追踪间隔（→500m），后车重新独立请求 MA 并恢复独立 T2T 位置报告。

**预期输出：**
- 图表类型：sequenceDiagram（两列 VOBC 间密集 T2T 通信）
- 核心 FR："ATS 虚拟编组指令下发"、"T2T 编组协商与制动能力验证"、"CACC 协同跟驰控制"、"编组后资源占用统一管理"、"解编与独立 MA 恢复"
- 涉及系统：VOBC_A、VOBC_B、ATS、TSM、T2T模块
- 难度：TACS 特有场景，验证分布式编组协商与制动能力校验

### TC-029 TACS 全自动无人驾驶（UTO/GoA4）综合场景 ★★★

**场景描述：**
TACS 列车在全自动无人驾驶（GoA4）模式下从始发站发车。列车在站台完成自动发车条件检查：全部站台门关闭锁紧、VOBC 自检正常（含障碍物检测系统 ODS、烟火检测传感器）、牵引使能有效。列车自动发车。运行中，ODS（雷达+视觉）检测到前方轨道 100m 处有异物（坠落的施工材料）。VOBC 即时计算异物与本车碰撞时间（TTC）小于 2s，触发 ATP 紧急制动，列车在异物前 30m 停稳。VOBC 通过 T2T 向后车发送前方异物告警。后车提前减速并在安全距离停车。TSM 更新线路资源状态，将该区段标记为障碍占用。远程调度中心通过无线通信切换到 CTC 模式：调度员调取车载摄像头画面确认异物为小型工具包，判断列车可以碾过（TACS 自动判断也可自主决策）。调度员下发低速碾压通过指令（5km/h），VOBC 以 EUM 模式低速通过后恢复 CAM。全部运营中断时间约 3 分钟。若检测到火灾烟雾，VOBC 自动停靠最近车站并执行疏散。

**预期输出：**
- 图表类型：sequenceDiagram + flowchart（综合型）
- 核心 FR："全自动发车条件检查"、"ODS 障碍物检测与紧急制动"、"T2T 后车前方告警传递"、"异物远程识别与处置决策"、"低速碾压通过模式"、"火灾烟雾检测与自动疏散"
- 涉及系统：VOBC、TSM、ODS、远程调度中心、T2T模块
- 难度：司机缺席，全部决策由 VOBC+远程调度完成

### TC-030 TACS 车站联锁接口交互 ★★☆

**场景描述：**
某 TACS 升级线路的车站仍保留传统的计算机联锁（CBI）作为安全防护后备。TACS 列车进入车站前，VOBC 通过 T2T 确认前方区段资源占用情况，同时向 TSM 发送进站请求。TSM 通过标准化接口（如 RSSP-I 协议）向 CBI 发送进路建立请求（含进路 ID：3G→4G，经由 6 号道岔反位）。CBI 执行传统联锁检查：敌对进路未建立、4 股道空闲、6 号道岔可转动。检查通过后 CBI 操纵 6 号道岔转换至反位并锁闭，开放进站信号机。CBI 向 TSM 返回进路建立成功 + 道岔状态 + 信号机状态。TSM 将 CBI 结果与 TACS 资源模型进行一致性比对：确认 CBI 锁闭的道岔与 TACS 资源管理器锁定的道岔一致。比对通过后 TSM 向 VOBC 下发进路确认通知，VOBC 自主计算并执行进站 MA。若 CBI 返回进路建立失败（如 6 号道岔失表），TSM 标记该进路不可用并通知 VOBC 选择替代进路或停稳等待。

**预期输出：**
- 图表类型：sequenceDiagram
- 核心 FR："TACS 进路请求上送"、"CBI 进路联锁检查与执行"、"进路状态一致性比对"、"VOBC 进站 MA 自主计算"、"CBI 道岔故障降级处理"
- 涉及系统：VOBC、TSM、CBI、OC
- 关键验证：TSM↔CBI 双模型一致性比对 + 传统联锁与 TACS 资源管理共存

---

## 十二、系统融通场景

### TC-031 ATS 全局调度：CBTC+TACS 混跑时刻表调整 ★★★

**场景描述：**
某 TACS 升级过渡线路同时运营 CBTC 列车和 TACS 列车。ATS 系统统筹管理两类列车的运行图。早高峰因上行线前方 CBTC 列车发生 ATO 故障降级至 ATPM（限速 45km/h），导致后续一列 TACS 列车追踪间隔缩小至安全阈值以下。ATS 自动检测到该冲突：TACS 列车正常追踪间隔为 90s，现缩短至 50s（低于 60s 阈值）。ATS 触发全局时刻表调整：对故障 CBTC 列车释放 2 分钟冗余时间，对后续 TACS 列车在原定下一站增加 1 分钟停站时间吸收延误，对 TACS 下游第二列 CBTC 列车不做调整（间隔充足）。调整方案经调度员一键确认后，ATS 分别以 CBTC 格式和 TACS 格式下发调整指令：对 CBTC 列车经 ZC→VOBC 链路下发 ATP 运行等级调整；对 TACS 列车经 TSM→T2T 链路下发推荐速度调整。ATS 持续监控列车恢复状态，待 CBTC 列车 ATO 自恢复后重新压缩间隔。

**预期输出：**
- 图表类型：flowchart（含多分支决策）
- 核心 FR："CBTC/TACS 统一时刻表管理"、"冲突检测与跨制式调整计算"、"双通道（ZC+TSM）指令差异化下发"、"调整效果持续监控与恢复"
- 涉及系统：ATS、ZC、TSM、CBTC VOBC、TACS VOBC、T2T模块
- 难度：跨系统协调 + 双格式指令分发 + 全局最优

---

## 评分指标更新

### 测试集统计

| 类别 | 场景数 | TC ID |
|------|--------|-------|
| CBI（计算机联锁） | 3 | TC-001~003 |
| TCC（列控中心） | 3 | TC-004~006 |
| RBC（无线闭塞中心） | 2 | TC-007~008 |
| CTC（调度集中） | 2 | TC-009~010 |
| 信号基础设备 | 2 | TC-011~012 |
| 综合场景 | 2 | TC-013~014 |
| CBTC 基础 | 3 | TC-015~017 |
| TACS 基础 | 3 | TC-018~020 |
| CBTC 进阶 | 5 | TC-021~025 |
| TACS 进阶 | 5 | TC-026~030 |
| 系统融通 | 1 | TC-031 |
| **总计** | **31** | |

| 指标 | 说明 | 权重（调整后）|
|------|------|----------------|
| 场景覆盖率 | 覆盖 CBI/TCC/RBC/CTC/基础设备/CBTC/TACS/混跑的比例 | 20% |
| 需求完整性 | FR 覆盖所有关键系统能力的比例 | 15% |
| 粒度合理性 | FR 为系统级能力而非 UI 操作步骤 | 10% |
| 图表类型匹配 | diagramType 选择是否贴合场景 | 10% |
| 异常路径覆盖 | 是否包含故障/降级等异常处理需求 | 10% |
| CBTC 子系统识别 | 是否正确识别 ATS/ATO/ATP/ZC/VOBC 角色 | 5% |
| TACS 分布式逻辑 | FR 是否覆盖资源申请→分配→锁定→释放周期 | 5% |
| 虚拟编组识别能力 | 是否正确识别 T2T 编组协商/制动校验/CACC | 5% |
| GoA 等级匹配 | 自动驾驶等级（GoA1~4）是否与场景匹配 | 5% |
| 降级模式完整度 | 是否覆盖 3+ 级降级路径 | 5% |
| 多制式统一调度 | 混跑场景是否正确处理 CBTC/TACS 双指令通道 | 5% |
| 安全包络处理 | MA/资源 FR 是否包含最不利位置偏移和安全余量 | 5% |

---

## 数据来源

本测试集基于以下公开资料整理：

1. CTCS-3 级列控系统运营场景功能需求分析（AMiner）
2. CTCS-3 级列控系统主要运营场景（行业培训教材）
3. 计算机联锁系统原理与技术条件（中国通号/铁科院）
4. RBC 无线闭塞中心功能需求（行业标准）
5. CTC 调度集中系统技术条件（铁道行业标准）
6. EN 50129 / IEC 62425 铁路信号安全相关系统标准
7. TB/T 3516-2018 CTCS-2 级列控系统总体技术要求
8. IEC 62290-1/2/3 城市轨道交通列车控制系统标准
9. T/CITSA 23-2022 城市轨道交通信号智能综合运维系统技术规范
10. IEEE 1474.1-2021 CBTC 性能与功能需求标准
11. T/CAMET 04001-2023 城市轨道交通全自动运行系统技术规范（GoA4/UTO）
12. 城市轨道交通 TACS 列车自主控制系统技术白皮书（通号院/铁四院）
13. 新线 CBTC/TACS 混合运营过渡期信号系统接口设计规范（行业指南）
